@model List<todo_list.Models.monthly>
@{
    ViewBag.Title = "monthshow";
    Layout = "~/Views/Shared/_layout.cshtml";
}
<h1 style="width: 1200px; margin-left: 350px; text-align: center; margin-top: 100px; background-color: gray; margin-top: 100px">My Monthly Target</h1>

<table class="table table-dark table-hover" style="width:1200px;margin-left:350px;text-align:center;">

    <tr style="font-size:xx-large">
        <td>ID</td>
        <td>Name</td>
        <td>Date</td>
        <td></td>
        <td></td>
        <td></td>
    </tr>

    @foreach (var x in Model)
    {
        <tr>
            <td>@x.Id</td>
            <td>@x.Name</td>
            <td>@x.Date</td>
            <td>@Html.ActionLink("Delete", "delmonth", null, new { Id = x.Id }, new { @class = "btn btn-danger" })</td>
            <td>@Html.ActionLink("Update", "updatemonth", new { Id = x.Id }, new { @class = "btn btn-primary" })</td>
            <td>@Html.ActionLink("Done", "donemonthly", new { Id = x.Id }, new { @class = "btn btn-success" })</td>

        </tr>
    }

</table>

